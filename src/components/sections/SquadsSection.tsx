import { SectionPlaceholder } from "@/components/sections/SectionPlaceholder";
import { sections } from "@/data/sections";

export function SquadsSection() {
  return <SectionPlaceholder section={sections.squads} />;
}
